enum PostCommentApiStatus {
  posting,
  success,
  failure,
  deleting,
  failedToDelete,
}

enum ApiStatus {
  initial,
  loading,
  success,
  failure,
}

enum AppEnv {
  local,
}

enum LogLevel {
  verbose,
  debug,
  info,
  warning,
  error,
  wtf,
}
